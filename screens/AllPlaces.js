import { StyleSheet, Text, View } from "react-native";
import React from "react";

import PlacesList from "../components/places/PlaceItem";

const AllPlaces = () => {
  return <PlacesList />;
};

export default AllPlaces;

const styles = StyleSheet.create({});
